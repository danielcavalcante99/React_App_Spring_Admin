import IconSpring from "../icons/IconSpring";

const NavBar = () => {
  return (
    <nav className="navbar navbar-light bg-dark">
      <div className="container-fluid">
        <span className="navbar-brand mb-0 h1 text-white">
          <IconSpring /> Spring Admin
        </span>
      </div>
    </nav>
  );
};

export default NavBar;